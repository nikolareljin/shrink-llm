# Benchmarking Guide

Benchmark outputs belong in `benchmarks/results/` as JSON and Markdown so model
quality, latency, and memory regressions can be compared across runs.

This document is a placeholder for the detailed benchmarking workflow referenced by the README.
