# Benchmarks

This directory stores benchmark tooling, small evaluation subsets, and published
benchmark results.

- `datasets/` contains small benchmark fixtures and eval subsets.
- `results/` contains JSON and Markdown benchmark outputs that may be committed
  when they represent a reproducible project baseline.

Avoid storing large raw datasets or device-local scratch outputs here.
