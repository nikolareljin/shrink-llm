# ONNX Runtime Mobile

This directory is reserved for ONNX Runtime Mobile optimization and packaging helpers.

Keep generated `.ort` artifacts out of version control unless they are an intentional release asset.
