# Datasets

This directory contains task-specific dataset download and preprocessing code.

- `ocr/` is reserved for OCR dataset tooling and manifests.
- `legal/` is reserved for legal document dataset tooling and manifests.
- `baby_cry/` is reserved for audio dataset tooling and manifests.

Large raw artifacts should remain ignored and must not be committed.
